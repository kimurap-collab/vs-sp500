# RSI-30枠 実装指示書（Fable起草 2026-08-17・SPEC.md追補第5弾）

vs-sp500プロジェクトに**2本目の戦略枠**を追加する。本体（配分戦略）とは独立に走り、同じベンチマーク（VOO握りっぱなし）と戦う。

## 仕様の出典（大将の発言）
- **戦略の追加**: 「RSIが30を切った会社を買うという仮想トレードをmoomooでやりたい」「同じプロジェクトで別枠でやろうよ。RSI-30 vs S&P500」（2026-08-17）
- **売買ルール全文**: 大将が完成した仕様として提示（同日）:
  ```
  ENTRY: RSI(14) <= 30 → BUY 1,000,000 yen
  PYRAMID: +2.5% from initial entry → BUY 500,000 / +5.0% → 250,000 / +7.5% → 250,000
  STOP: -8% from initial entry → SELL ALL
  NORMAL PROFIT: +20% from average cost → SELL 50% / +25% → SELL REMAINING
  EXCEPTION: If +20% reached within 15 trading days: DO NOT TAKE PROFIT, HOLD until 8 weeks from initial entry
  ```
- **通貨と金額**: 「１）ドルでいい」「１銘柄5万ドルスタートにしよう」（同日）→ 元ルールの比率を保って$50,000/$25,000/$12,500/$12,500
- **資金**: 「２）半分ずつ了解。それでいこう」（口座100万ドルの半分＝**$500,000**）
- **銘柄数の上限**: 「２）銘柄数制限なし」（同日）
- **対象銘柄**: 「３）sp500銘柄・nasdaq100銘柄で」（同日）
- **損切りの扱い**: 「４）しっかり損切り８%で実行」（8週ホールド中も有効）
- **伸ばす玉**: 「５）…25%残るよね？それは全株hold」＋「利益が出ている場合は適用しない」（同日）
  → 最後の25%は売らず保持。利益が出ている間は損切りを適用しない
- **資金不足時の優先順位**: 「２）１で」＝**RSIが低い順**（同日）
- **再エントリー**: 「６）気にせずrsi30切りしたら買い」（クールダウン無し）
- **バックテストを行わず直接運用する判断**: 「何をやりたいの？それをやって俺に何を伝えようとしてる？」の指摘を受けFableが再考し、
  「バックテストはやめて実運用の枠を作る」提案に大将が「いけ」（同日）

## 確定ルール

| 段階 | 内容 |
|---|---|
| エントリー | RSI(14) ≤ 30 → **$50,000**分を買い（整数株・切り捨て） |
| 買い増し（初期エントリー価格基準） | +2.5%→**$25,000** / +5.0%→**$12,500** / +7.5%→**$12,500**（1銘柄上限$100,000） |
| 損切り | 株価が初期エントリー価格 × 0.92 以下 → **全株売却**（8週ホールド中も有効） |
| 利確1 | 平均取得単価 × 1.20 以上 → **総株数の50%**を売却 |
| 利確2 | 平均取得単価 × 1.25 以上 → **総株数の25%**を売却 |
| 伸ばす玉 | 残る**総株数の25%は売却しない**（無期限保有）。利確の対象外 |
| 例外 | 初期エントリーから**15営業日以内**に平均取得単価×1.20へ到達した場合、利確1・2を**初期エントリーから8週間（56暦日）まで停止**。期間経過後は通常の利確ルールに戻る |
| 再エントリー | クールダウン無し。伸ばす玉を保有中でも、再びRSI≤30なら**別ロットとして新規建て** |
| 資金不足時 | **買い増しを優先**し、その後に新規エントリーを**RSIが低い順**に、現金が足りる分だけ実行 |

## 対象銘柄（ユニバース）
- **S&P500 ＋ NASDAQ100 の構成銘柄**（重複は排除）
- 取得方法: moomooの `get_plate_stock` を試し、取得できなければ他の経路（Wikipedia等）で取得する。
  **結果は `universe.json` にキャッシュし、月初のみ更新する**（毎日取り直さない）
- 取得元と取得日を `universe.json` に記録すること

## 資金とベンチマーク
- 初期資金 **$500,000**
- ベンチマーク: **初回実行日の終値で$500,000をVOOへ一括投資**して放置（コストゼロ・コードで計算）
- `bench_units_rsi = 500000 / VOO終値`。以後この値は不変

## 対象ファイル
- 新規: `rsi_strategy.py`（ルールの実装）、`ledger/rsi/portfolio.json`・`trades.csv`・`history.csv`、`universe.json`
- 修正: `daily_run.py`（RSI枠の実行を追加）、`portfolio.py`（**保有照合を本体＋RSI枠の合計に変更**）、
  `report.py`・`index.html`（三つ巴表示）、`config.py`（RSI枠の定数）
- **編集禁止**: `charter.md` / `LOOP_PROMPT.md` / `SPEC*.md` / `theses.json` / `candidates/` / `ledger/`直下の本体台帳

## 重要な制約

### 1. 本体（vs-sp500）を壊さないこと
- 本体の台帳・配分・発動条件・ベンチマーク・勝敗記録には**一切触れない**
- **保有照合の変更が最大の危険箇所**。現在は「本体の台帳 == moomooの実保有」を検証しているが、
  RSI枠が同じ口座に建玉を持つため、**「本体 + RSI枠の合計（銘柄ごとに合算） == moomooの実保有」**に変更する。
  変更後、本体だけの状態でも照合が通ることを必ず確認する

### 2. 現金は口座の実増減で按分する
- moomooの現金は口座で1つだが、台帳は枠ごとに別々に持つ
- 発注の前後で `broker.get_cash()` を取り、**その差分を発注した枠の現金に反映**する（本体と同じ方式）
- 手数料のモデル化は行わない

### 3. ポジションはロット単位で管理する
同一銘柄に複数ロットが並びうる。各ロットが保持する状態:
`ticker / initial_entry_date / initial_entry_price / 買い増し実施済みフラグ(3段) / 保有株数 /
 総投入額 / 平均取得単価 / 利確1済み / 利確2済み / 例外フラグ / 8週期限日`

### 4. 実口座への発注は絶対禁止
`TrdEnv.SIMULATE` / `acc_id=5338087` のみ。

## 実装の順序（必ずこの順で）
1. `rsi_strategy.py` のルール実装と**単体テスト**（後述）
2. `daily_run.py` への組み込み（本体の後にRSI枠を実行）
3. 保有照合の合算対応
4. 管理画面の三つ巴表示
5. 初回実行（`--dry-run` で確認 → 本実行）

## 検証（すべて実施し実出力を報告）
1. **ルールの単体テスト**（実データ不要・作った価格列で判定する）:
   - a. エントリー後に+2.5%/+5%/+7.5%と上昇 → 買い増しが3回入り、総額$100,000・平均取得単価が正しいこと
   - b. エントリー後に-8%到達 → 全株売却されること
   - c. 買い増し後に平均取得単価+20% → 総株数の50%売却、+25% → さらに25%売却、**25%が残ること**
   - d. **10営業日目に+20%到達** → 利確されず、初期エントリーから56日後まで保持されること
   - e. dの期間中に-8%到達 → **損切りが実行されること**
   - f. 伸ばす玉のみ保有中に再びRSI≤30 → 別ロットとして新規建てされること
2. 現金不足時にRSIの低い順で選ばれること（信号5件・現金2件分で検証）
3. **本体の非破壊確認**: `daily_run.py --dry-run --no-loop` が従来どおり正常終了し、本体のNAV・保有・判断が変わらないこと
4. 保有照合が「本体+RSI枠の合計」で成立すること（RSI枠が空の状態でも通ること）
5. 初回実行後の `ledger/rsi/portfolio.json` の中身
6. 管理画面に本体・RSI枠・S&P500の3本が表示されること

## 禁止事項
- 実口座への発注
- 本体の台帳・憲章・ベンチマーク・`bench_units`・`start_date` の変更
- charter.md / LOOP_PROMPT.md / SPEC*.md / theses.json / candidates/ の編集
- Telegram送信（0通。環境変数 `VS_SP500_DEFER_TELEGRAM=1` を立てて実行すること）
- vs-sp500外への変更
- 完了したら git commit まで（pushは不要）

## 報告フォーマット
- 変更・新規ファイルと変更概要／検証1〜6の実出力／**本体が無傷であることの証明**／仕様との差分欄

---

# 改訂履歴

## 2026-08-18 改訂（大将承認済み）

**発端**: RSI(14)の計算が**単純移動平均**になっていた（正しくはワイルダー平滑）。
大将のTradingViewがAAPL 43.41を示すのに対し、うちの実装は22.04を返していた。
この誤差でRSI30割れの候補が64件も湧いていた（正しくは6件）。**発注前に発覚したため実害なし**。
発見の経緯は大将の指摘「rsiは期間１４日、終値ベースで考えてるが君はどうだ？俺のトレーディングビューではappleのrsiは現在43.41だ」（2026-08-17）。

**確認済みの事実**: moomooのRSIもワイルダー方式。同一終値でmoomoo 43.551 / TradingView 43.41 の差は
計算式ではなく**配当調整の有無**（前復権QFQ か 無調整か）によるもので、無調整で計算すると43.407となりTradingViewと一致する。
32という線引きでは両方式で選ばれる銘柄は同一だったため、**moomooの値をそのまま採用する**。

| 項目 | 旧 | 新 |
|---|---|---|
| RSIの取得元 | yfinance＋自前計算（単純移動平均・**誤り**） | **moomooのスクリーナー**（ワイルダー・前復権） |
| 候補抽出 | ユニバース518銘柄をyfinanceで一括取得 | **moomooスクリーナー1回**（RSI閾値＋時価総額10億ドル以上で1ページに収める） |
| エントリー閾値 | RSI(14) ≤ 30 | **RSI(14) ≤ 32** |
| 初回買付 | $50,000 | **$30,000** |
| 買い増し | $25,000 / $12,500 / $12,500 | **$15,000 / $7,500 / $7,500**（比率4:2:1:1は不変） |
| 1ロット上限 | $100,000 | **上限という概念を置かない**（比率どおり積むだけ） |
| 個別株の配当 | 現金に加算 | **無視**。ただし**ベンチマークのVOO配当再投資は従来どおり継続**（勝敗の基準線のため） |

### 仕様の出典（大将の発言・2026-08-18）
- 閾値32: 「rsi30は厳しい基準なのでrsi32から購入することにする」
- 金額: 「これだけ少ないなら予算をアップする。3万ドルでいく」
- 買い増し比率: 「買う時は3万ドル買え。そして上昇すれば比率分買い増せ」＋択一（１）＝4:2:1:1を維持
- 上限の廃止: 択一（１）「上限は設けず、比率どおり積むだけ」
- moomoo直取り: 「なんで正解がmoomooに決まってるのにうちの計算式と照らし合わす必要があるねん」「正確にmoomooのを使え。君の手心は不要だ」
- API1回化: 「apiを3回も走らす理由はなんだ？　俺は無駄が嫌いだし、無駄な費用も払いたくない」
- 配当の無視: 「ざっくりrsiの勝率を知りたいだけだから、配当とか細かいのは無視でもいい」

### 本体（配分戦略）への影響
- 本体の押し目買い条件 **RSI(14) < 30 は変更していない**（RSI枠の32とは別物）
- ただしRSIが正しくなった分、本体の押し目買いは従来より発動しにくくなる（バグで不当に低く出ていたため）
- 本体のVOO用RSIもmoomooのK線から取得する方式に変更（71.2 → 62.0）。NAV・ベンチマーク・保有は不変であることを確認済み

## 2026-08-19 改訂（大将承認済み・方針転換）

**削除したもの**:
- RSI枠の損切りルール（初期エントリー価格から-8%で全株売却）
- 例外条項の「8週ホールド中も損切りは有効」に相当する記述・実装（損切り自体が無くなったため意味を失う）

**残したもの（一切変更なし）**:
- 利確1（平均取得単価×1.20で総株数の50%売却）
- 利確2（平均取得単価×1.25でさらに25%売却）
- 伸ばす玉（残り25%は無期限保有）
- 例外条項（15営業日以内に+20%到達→初期エントリーから56暦日まで利確停止）
- 買い増し（+2.5%→$15,000 / +5%→$7,500 / +7.5%→$7,500）
- 同一銘柄1ロット制限

### 仕様の出典（大将の発言・2026-08-19）
> 「確かに-8%刺さる可能性あるな。売りのルールは削除しようか。『rsi32以下の銘柄を買うと本当に儲かるのか』を知りたいもんな。損切りルールは削除して。利益出てから売るルールはそのまま継続で。」

**背景**: 買った5銘柄のうち4銘柄が52週安値の目前にあり、-8%の損切りが早期に発動する可能性が高い。損切りを挟んだままでは「RSI32以下で買う」ではなく「RSI32以下で買って8%で切る」の成績を測ることになり、`GOAL.md` の合格条件「俺のrsi理論が正しければ実弾でトレード」の判定材料にならない。

**方針転換であること**: 大将は以前「４）しっかり損切り８%で実行」（2026-08-17）と指示していた。今回はその方針転換である。

### 測定上の偏り（2026-08-19記録）
- 損切りが無くなり下値は無制限になる一方、利確は残るため75%の玉は+25%までしか取れない。**RSI理論の実力は実際より悪く出やすい**（`GOAL.md`に記録済みの配当未計上の偏りとは逆向き）
- 損切りが無いため下落銘柄が資金を拘束する。RSI枠の現金は約$350,355で1銘柄$30,000のため**あと約11銘柄で新規エントリーが打ち止めになる**。検証できる件数が減る
